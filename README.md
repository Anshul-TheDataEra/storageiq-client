# StorageIQ Client Scanner

Durable-Functions SharePoint storage/version scanner that runs inside the
customer's own Azure tenant. It scans SharePoint (recursive, paginated,
throttle-aware, resumable) and sends only anonymised aggregate numbers to the
StorageIQ Intelligence API, which returns the savings + ranking.

See **DEPLOY-TO-CLIENT.md** for the full step-by-step deployment guide.
